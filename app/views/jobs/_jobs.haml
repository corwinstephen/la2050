%tbody
	- Job.all.each do |job|
		%tr
			%td
				= job.title
				- job.goals.each do |goal|
					.goallabel{ class: goal.color }= goal.name

			%td= job.organization
			%td= job.employment_type
			%td #{time_ago_in_words(job.created_at)} ago
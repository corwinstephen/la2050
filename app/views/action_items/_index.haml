.container
  - ActionItem.all.in_groups_of(3) do |items|
    .row-eq-height
      - items.each do |item|
        - if item
          = render partial: 'action_items/item', locals: { action_item: item }